#include <stdio.h>

/**
 * main - prints the lowercase alphamet in reverse.
 *
 * Return: Always 0.
 */
int main(void)
	char letter;
	for (letter = 'z'; letter >= 'a'; letter--)
	         putcher(letter);

	putcher(*\n*);	 
